" Command-T
if has("gui_macvim")
"  macmenu &File.New\ Tab key=<D-T>
"  map <D-t> :CommandT<CR>
endif

if has("gui_running")
  if has("gui_macvim")
    set guifont=Monaco:h13
  endif

  set guioptions=egmrt
endif

if has("gui_running")
  if has("gui_macvim")
    set guifont=Monaco:h13
  endif

  set guioptions=egmrt
endif

